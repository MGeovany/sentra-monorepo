# Sentra CLI

Developer-first CLI to scan, stage, and push `.env*` files.

## Install (Homebrew)

```sh
brew tap MGeovany/tap
brew install sentra
```

## Quickstart

```sh
sentra login
sentra scan
sentra add .
sentra commit -m "sync envs"
sentra push
```

## Docs

- CLI commands: `cli/README.md`
